# ALI RAG V3 — core dependencies.
# Core is deliberately light (§89): stdlib + numpy + yaml run the whole
# retrieval stack. Parsers/servers are optional extras — a missing one
# degrades that file type to FAILED/UNSUPPORTED, never crashes the run.

numpy>=1.26
PyYAML>=6.0

# ── document parsers (install what the corpus needs) ─────────────────────────
PyMuPDF>=1.24          # PDF text + page rendering
python-docx>=1.1       # DOCX
openpyxl>=3.1          # XLSX
python-pptx>=0.6.23    # PPTX
pytesseract>=0.3.10    # OCR (requires the Tesseract binary + eng/msa data)
Pillow>=10.0
ezdxf>=1.3             # DXF read-only harvest (never writes CAD)

# ── structured PDF parsing (GPU-capable; reuses V1's proven stack) ───────────
# docling==2.107.0     # heavyweight — install per V1 root requirements.txt

# ── API server ───────────────────────────────────────────────────────────────
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6

# ── optional vector database (primary candidate, §18; memmap fallback built in)
# qdrant-client>=1.9

# ── dev/test ─────────────────────────────────────────────────────────────────
pytest>=8.0
